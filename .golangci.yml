# .golangci.yml
run:
  timeout: 5m
  go: "1.22"

linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - unused
    - gosimple
  disable:
    - typecheck

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck